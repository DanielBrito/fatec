#include<stdio.h>
#include<conio.h>
int main()
{
    int index = 0, contador;
    char letras[5] = "Joao";
    for (contador=1; contador < 1001; contador++)
    {
        if (contador>3)  index=0;
        printf("%c\n", letras[index]);
    if (contador%5==0);
    }
    printf("\n");
    {
        (index==3) ? index=0: ++index;
    }
   getch();
   
}

